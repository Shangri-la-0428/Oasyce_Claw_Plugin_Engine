# Oasyce Protocol FAQ

> 面向投资人、合作方和技术评审的常见问题解答。
> 最后更新：2026-03-21

---

## 一、安全与钱包

### Q1: 如何注册钱包？支持冷钱包吗？

Oasyce 协议不绑定特定钱包实现。任何支持 Ed25519/Secp256k1 签名的钱包均可使用。

**推荐方案：**
- 硬件钱包（Ledger / Trezor）生成 seed phrase，离线保管
- 应用层使用 MetaMask 或 Keplr 作为签名界面
- 多设备：主设备签名，辅助设备只做查询

**多签方案（高价值场景）：**
- 2/3 多签：一个硬件钱包 + 一个备用设备 + 一个离线备份
- Cosmos SDK 原生支持 multisig（`oasyced keys add --multisig`）

**架构支持：** ✅ 完整。协议层不感知钱包类型，只验证签名。

---

### Q2: 安全防护建议？

| 层级 | 措施 |
|------|------|
| 私钥 | 硬件钱包 + 离线 seed phrase（纸/钢板） |
| 账户 | PIN + passphrase（可选） |
| 交易 | 签名前确认金额和接收方 |
| 节点 | TLS + 防火墙 + 仅暴露必要端口 |
| 应用 | API token 认证 + localhost 限制 |

---

## 二、数据访问模型

### Q3: 我只想访问数据，不想长期持有份额，怎么办？

**这是 Oasyce 的核心设计优势。** 两种模型并存：

| 模型 | 适用场景 | 成本结构 |
|------|----------|----------|
| **Share（份额）** | 长期持有、参与收益分成 | 买入 bonding curve 价格，持续 exposure |
| **Access（访问）** | 一次性使用、时效性数据 | 锁定 bond → 使用 → 释放 bond |

**Access 流程：**
```
1. 查询报价 → oasyce access quote <asset_id>
2. 锁定 bond → 系统锁定临时保证金
3. 获取数据 → L0(查询)/L1(采样)/L2(计算)/L3(交付)
4. 完成 → bond 自动释放
```

**Bond 定价公式：**
```
Bond = TWAP × LevelMultiplier × RiskFactor × (1 - RepDiscount) × ExposureFactor
```

信誉越高，bond 越低。这是"信任货币化"的核心机制。

**架构支持：** ✅ 完整。`DataAccessProvider` + `OasyceServiceFacade.access_buy()` 已实现。

---

### Q4: Access Level 分级是怎么工作的？

| 级别 | 权限 | Bond 倍数 | 锁定期 |
|------|------|-----------|--------|
| L0 查询 | 聚合统计，数据不离开安全区 | 1× | 1 天 |
| L1 采样 | 脱敏水印样本片段 | 2× | 3 天 |
| L2 计算 | 代码在 TEE 执行，仅输出离开 | 3× | 7 天 |
| L3 交付 | 完整数据交付 | 5× | 30 天 |

信誉门控：R<20 只能 L0，R<50 只能 L0/L1，R≥50 全部开放。

---

## 三、份额与控制权

### Q5: 持有份额越多，是否应该拥有更多控制权？比如删除资产？

**明确回答：不。**

Oasyce 的核心设计原则是 **Shares = 经济权，不等于控制权**。

**为什么不能让大股东删除/控制资产：**
1. **攻击模型**：攻击者买入 51% 份额 → 删除资产 → 其他持有人归零
2. **破坏退出机制**：如果大户能关闭资产，graceful exit 的可信性就不存在了
3. **本质区别**：数据资产是不可变对象，不是公司。公司股权=治理权，数据份额=收益权

**大股东可以获得的权益（经济型，非控制型）：**
- 更低的 access bond（信誉折扣）
- 更高的收益分成比例
- 信号权（推荐/标记版本，不强制执行）

**不可授予的权利（底线）：**
- ❌ 删除资产
- ❌ 修改内容
- ❌ 强制迁移
- ❌ 绕过 cooldown 关闭

**架构支持：** ✅ 已实现。`delete_asset` 只允许 owner 在 TERMINATED 且无 unclaimed 时操作。lifecycle 有强制 cooldown。

---

## 四、数据完整性与存储

### Q6: 如果 owner 修改了文件内容怎么办？

**链上存的是 content hash，不是文件本身。**

```
注册时：metadata_hash = SHA-256(文件内容)
链上记录：不可变
```

| 操作 | 结果 |
|------|------|
| 修改文件内容 | hash 改变 → 必须注册为新资产 |
| 原资产 | 不受影响，hash 永远匹配原始内容 |
| 验证 | 任何人可以 `oasyce fingerprint extract` 验证完整性 |

**架构支持：** ✅ 完整。`file_hash` 在注册时写入，不可更改。

---

### Q7: 数据存储是单节点还是分布式？

**当前架构（Phase A-B）：**
```
链上：hash + metadata（不可变）
链下：provider 本地存储
```

**路线图（Phase D）：**
```
链上：hash + metadata（不变）
链下：IPFS / Arweave + 可选加密分片
```

| 存储层 | 适用场景 | 状态 |
|--------|----------|------|
| 本地存储 | 开发/测试/小规模 | ✅ 当前 |
| IPFS | 公开数据，内容寻址 | 📋 Phase D |
| Arweave | 永久存储，高价值数据 | 📋 Phase D |
| 加密分片（Shamir） | 私有模型权重，高安全 | 📋 Phase D |

**关键点：** 存储层的变化不影响链上协议。链上只关心 hash，不关心数据在哪。

---

## 五、资产版本与演化

### Q8: 资产更新了怎么办？旧版本放在哪？

**核心原则：Asset = immutable。更新 = 新资产。**

```
Asset V1 (hash_abc) → 永久存在
Asset V2 (hash_def) → 新资产，链接到 V1
```

**版本链接：**
- 数据库已有 `asset_versions` 表，记录版本历史
- 每个版本有 `prev_hash` 链接到上一版本

**旧版本：**
- 永久保留在链上（不可删除）
- 可以自然降价（持有人卖出 → bonding curve 价格下降）
- Owner 可以发起 shutdown → 持有人 claim 退出

---

### Q9: V2 发布后，V1 的价值怎么处理？

**两条路径：**

| 路径 | 机制 | 适用场景 |
|------|------|----------|
| **市场自然迁移** | 用户自行卖 V1、买 V2 | 大多数情况 |
| **Owner 主动退出 V1** | initiate_shutdown → cooldown → claim | V1 完全废弃 |

**为什么不做链上 burn→mint 迁移（V1 阶段）：**
1. 市场自然定价已经解决了价值转移问题
2. 强制迁移比例（如 100 V1 → 70 V2）需要预设"V2 比 V1 值多少"，这本身就是定价问题，不应由协议决定
3. 如果 V2 确实更好，V1 持有人会自然迁移；如果不迁移，说明 V1 对他们仍有价值

**未来可选（Phase D+）：**
- Owner 可设置 migration hint（建议迁移到 V2），但不强制
- 信号权：高信誉用户的推荐有更高权重

**架构支持：** ⚠️ 部分。版本表存在但 API 未暴露。需要补 facade 方法。

---

## 六、信誉系统

### Q10: 信誉如何产生？影响什么？

**信誉是行为驱动的，不可购买或转移。**

**产生方式：**
| 行为 | 信誉变化 |
|------|----------|
| 完成一次 access | +2 |
| 被成功挑战（提供虚假数据） | -10 |
| 数据泄露 | -50 |
| 时间衰减 | -5 / 90天 |

**信誉影响：**
| 维度 | 效果 |
|------|------|
| **Bond 折扣** | 高信誉 → 更低的保证金要求 |
| **Access 门控** | R<20 限制 L0，R<50 限制 L0/L1 |
| **信号权重** | `weight = log(1 + reputation)` |

**不影响：**
- ❌ 资产控制权
- ❌ 份额投票权
- ❌ 强制操作

**架构支持：** ✅ 链上 + Python 均已实现。Python 端需要持久化（当前内存存储）。

---

## 七、生命周期与退出

### Q11: 用户的资产（份额）会不会"消失"？

**不会。** Oasyce 有完整的 Graceful Exit 机制：

```
ACTIVE → owner 发起 shutdown → COOLDOWN（7天）→ TERMINATED → holder claim 退出
```

- Cooldown 期间：不能新增购买，但可以卖出
- Terminated 后：每个持有人按比例 claim 快照时的 reserve
- Claim 是 pull-based：持有人主动领取，防止 gas 爆炸

**架构支持：** ✅ 完整。`initiate_shutdown` / `finalize_termination` / `claim_termination` 已实现。

---

### Q12: 数据会不会被篡改？

**不会。**
- 链上只存 content hash
- 任何文件修改都会改变 hash → 成为不同资产
- 指纹和水印系统可追溯数据来源

---

## 八、系统覆盖度总结

| 投资人关注点 | 状态 | 说明 |
|-------------|------|------|
| 钱包安全 | ✅ | 协议层不绑定钱包，支持多签 |
| 不买份额只用数据 | ✅ | Access/bond 模型，核心差异化 |
| 数据不可篡改 | ✅ | hash + immutable asset |
| 可安全退出 | ✅ | Graceful Exit + pull-based claim |
| 份额不等于控制权 | ✅ | 经济权 only |
| 版本管理 | ⚠️ | 数据库 schema 存在，API 待补 |
| 存储层标准 | ⚠️ | 当前本地存储，Phase D 对接 IPFS |
| 价值迁移 | ✅ | 市场自然迁移 + owner shutdown |

---

## 九、技术规格速查

| 参数 | 值 |
|------|-----|
| 共识 | CometBFT (Tendermint) |
| SDK | Cosmos SDK v0.50.10 |
| 定价 | Bancor 连续绑定曲线 (CW=0.5) |
| 费率 | 93% provider / 5% protocol / 2% burn |
| Shutdown cooldown | 7 天 |
| 陪审团 | 5 人，2/3 多数 |
| 信誉衰减 | -5 / 90 天 |
| 最低信誉门槛 | R=20 (L0), R=50 (full access) |

---

*本文档基于 Oasyce Protocol v2.1.2 和 oasyce-chain Phase B 编写。*
